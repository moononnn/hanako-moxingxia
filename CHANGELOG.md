# 更新日志

## v0.1.17（2026-09-01）

**降级保护：主模型优先，备用救火**

- 降级计数修复：同一轮日志里多条失败只算 1 次的 bug（滚动摘要一次炸 3 条，计数却永远停 1，到不了阈值）→ 改为逐条累计，一轮多失败直接触发降级
- 恢复行误判修复：「恢复正常 / recovered」行（带错误原文）不再被当成新失败
- 快速失败切换：备用模型从「死磕 5 分钟」改为「临时救火约 1 分钟」，到点立刻切回主模型再试，试不动再切备用，循环
- 快照-恢复制：降级前把用户当前配置（utility / utility_large / vision 三槽）快照存下，恢复时写回原值而不是清空，充值后自动回到原模型，不用重选
- 识图兜底暂时隐藏：视觉模型行从降级面板隐藏（后端逻辑保留可随时启用），降级只切大小工具；原因：图片失败不打日志，日志轮询抓不到信号，且识别失败本身有明确用户反馈，无需静默兜底
- 备用模型选择改为两级联动（先选供应商再选模型），带「测试一下」连通按钮，视觉槽位只列能识图的模型
- 消耗量监测：页面展示近 24h 工具 / 插件 / 总 token 用量
- 联动提个醒：降级连续两轮时通过提个醒对外接口弹 Windows 通知（可选依赖，不装也能降级）
- 深度思考型备用兼容：备胎思考档位高 + 空响应时归因为「想太多没说出来」，不算真坏，不误弹通知

**其他**

- 模型 / 供应商显示名自定义：✎ 改名只动显示不动调用
- 供应商区分内置 / 自定义：内置标「Hana 内置」（可隐藏不可换位），自定义标「可排序」
- 滚动条主题化：细薄荷绿圆条替换系统默认灰条
- 开关（滑动块）样式与主题对齐：off 态轨道暖米色、knob 暖白、hover 反馈
- 页面 JS 引号转义修复（v0.1.7 残留，检查更新按钮文案）

## v0.1.7（2026-08-26 · 首次发布）

**核心功能**

- 供应商开关：隐藏后从模型菜单消失，配置与密钥原样保留，随时可恢复
- 供应商 / 模型拖动排序，保存后实时同步模型菜单
- 正在使用保护：正在用的供应商拒绝收入匣底
- 运行时对账：宿主未采纳自定义顺序时明确提示
- 补齐 provider-plugins 本地供应商的模型列表，不显示为 0 个模型
- 拖动到视口边缘自动连续滚动

**修复与兼容**

- minAppVersion 对齐 Hana v0.449.0 稳定版（原 0.450.0 会导致 449 上插件不加载）
- 模型列表独立滚动容器 + 定时器驱动，兼容 Hana iframe 不转发拖动滚动的情况

**体验**

- 检查更新按钮（后端对比 GitHub tags + 前端结果区）
- 反馈 / 提 issue 入口