# 模型匣发布契约

## 版本与变更

- 发布版本以 `manifest.json` 的 `version` 为准。
- 发布前把 `PENDING_CHANGES.md` 的已完成变更并入 `CHANGELOG.md`，再清空账本并保留标题。
- 发布标题要包含版本号和本版主要内容，不能只写版本号。

## 固定顺序

1. 检查独立提交节点、测试和语法。
2. 推送代码，等待 GitHub Actions 全部成功。
3. 生成干净发布目录和 zip，计算 SHA-256。
4. 对 zip 解压目录做安装路径、版本、隐私和命名红线检查。
5. 创建 GitHub Release，附 zip 和发布说明。
6. 发布后保留上一版产物，并提醒用户确认。

## 干净安装包

安装包只包含：

- `manifest.json`
- `package.json`
- `index.js`
- `lib/` 与 `routes/` 运行时代码
- `README.md`、`CHANGELOG.md`、`LICENSE`

不得包含 `.git/`、测试文件、项目日志、待发布账本、运行时数据、日志、备份目录或本机私有文件。zip 条目不得带 `./`、绝对路径或 `../`。

## 发布前红线

- 对发布副本运行语法检查、入口导入检查和自动测试。
- 用发布树对账脚本确认暂存目录与解压目录逐文件一致。
- 全库检查用户姓名、其他助手姓名、真实邮箱和本地测试入口。
- 确认运行时数据写入 Hana 的插件数据目录，不进入安装包。
- 公开发布前完成全新安装与旧数据升级检查。
